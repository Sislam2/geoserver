require "spec_helper"

describe Geoserver do
  it "has a version number" do
    expect(Geoserver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
